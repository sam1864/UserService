server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    ## driver-class-name: org.postgresql.Driver
    username: uatuser
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always   # auto-create SPRING_SESSION tables (dev/test)

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23liEUUod6bJMFBDYl
            client-secret: 8a75ca094e3ab7d43fa17d5645fbdb7ce83ad5ca
            scope: read:user
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
        provider:
          github:
            authorization-uri: https://github.com/login/oauth/authorize
            token-uri: https://github.com/login/oauth/access_token
            user-info-uri: https://api.github.com/user
springdoc:
  swagger-ui:
    oauth:
      client-id: Ov23liEUUod6bJMFBDYl
      client-secret: 8a75ca094e3ab7d43fa17d5645fbdb7ce83ad5ca
      use-pkce-with-authorization-code-grant: true
#      authorization-url: https://github.com/login/oauth/authorize
#      token-url: https://github.com/login/oauth/access_token
#      redirect-uri: http://localhost:8080/swagger-ui/oauth2-redirect.html
